_schema-version: 3.3.0
ID: field-service
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: field-service-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: field-service-auth
      - name: field-service-db
      - name: field-service-destination

  - name: field-service-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: field-service-db

  - name: field-service-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: field-service-auth
      - name: srv-api
      - name: field-service-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: fieldserviceadmin
          artifacts:
            - admin.zip
          target-path: app/
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet

  - name: fieldserviceadmin
    type: html5
    path: app/admin
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: field-service-destinations
    type: com.sap.application.content
    requires:
      - name: field-service-auth
        parameters:
          service-key:
            name: field-service-auth-key
      - name: field-service-html5-repo-host
        parameters:
          service-key:
            name: field-service-html5-repo-host-key
      - name: srv-api
      - name: field-service-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: field-service-html5-repository
              ServiceInstanceName: field-service-html5-repo-host
              ServiceKeyName: field-service-html5-repo-host-key
              sap.cloud.service: fieldservice.service
            - Name: field-service-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: field-service-auth
              ServiceKeyName: field-service-auth-key
              sap.cloud.service: fieldservice.service

resources:
  - name: field-service-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: field-service-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'admin (field-service ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.admin'
          - name: 'agent (field-service ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.agent'
  - name: field-service-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: field-service-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: field-service-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: field-service-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
